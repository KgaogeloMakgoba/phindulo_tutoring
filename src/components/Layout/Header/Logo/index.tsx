import Link from "next/link";

const Logo: React.FC = () => {
  return (
    <Link href="/" className="text-2xl font-bold text-primary">
      Phindulo Tutoring
    </Link>
  );
};

export default Logo;